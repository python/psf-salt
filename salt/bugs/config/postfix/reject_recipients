{% for tracker, config in pillar["bugs"]["trackers"].items() %}{% if not config.get("accept_email", False) %}
{{ config["config"]["tracker__email"] }} REJECT This tracker is in read-only mode. Please use GitHub issues to open a ticket.
{%- endif %}{%- endfor %}
