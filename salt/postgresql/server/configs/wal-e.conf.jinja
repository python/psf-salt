{% set swift_tenant = salt["pillar.get"]("wal-e:swift-tenant") %}

#------------------------------------------------------------------------------
# WRITE AHEAD LOG
#------------------------------------------------------------------------------

# - Archiving -

archive_mode = on
archive_command = 'SWIFT_TENANT="{{ swift_tenant }}" envdir /etc/wal-e.d wal-e wal-push %p'
archive_timeout = 60
